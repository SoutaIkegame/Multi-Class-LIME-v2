# Repository Instructions

## Purpose

このリポジトリは、研究室PCと自宅Macの間でGitHubを介して作業を引き継ぐ。
Codexはコードだけでなく、別端末で作業を再開するための文脈も常に保守すること。

## Handoff Documents

引き継ぎには次の2ファイルを使用する。

- `docs/PROJECT_SUMMARY.md`: プロジェクト全体の目的、現状、構成、重要な設計判断、既知の制約を記録する長期的なまとめ。
- `docs/RECENT_WORK.md`: 直近の作業内容、確認結果、未完了事項、次に行う具体的な作業を記録する短期的なまとめ。

## At the Start of Every Task

1. `docs/PROJECT_SUMMARY.md`を読む。
2. `docs/RECENT_WORK.md`を読む。
3. 実際のコード、設定、Gitの状態と記載内容が一致するか確認する。
4. 記載が古い、曖昧、またはコードと矛盾している場合は、コードと検証結果を優先し、相違点をユーザーへ伝える。
5. 未完了事項と次の作業を確認してから変更を開始する。

## Updating the Handoff

ユーザーが作業終了、引き継ぎ、commit、push、PR作成などを依頼した場合は、Git操作の前に2つのまとめを確認・更新する。

### `docs/PROJECT_SUMMARY.md`

プロジェクト全体に関わる永続的な情報だけを記載する。次の場合に更新する。

- 目的、対象範囲、主要機能が変わった。
- ディレクトリ構成やアーキテクチャが変わった。
- セットアップ方法、実行方法、主要コマンドが変わった。
- 重要な設計判断や依存関係が追加・変更された。
- 既知の制約、リスク、長期的な課題が変わった。

一時的な試行錯誤や細かな作業ログは書かない。既存内容を確認し、重複を避けて現在の正しい状態に更新する。

### `docs/RECENT_WORK.md`

原則として各push前に更新する。次の作業者がこのファイルだけでも再開地点を判断できる具体性を持たせる。

必ず次を記載する。

- 更新日時と作業環境
- 今回の目的
- 実施した変更と主要な変更ファイル
- 重要な判断とその理由
- 実行したテスト、確認コマンド、結果
- 未完了事項、既知の問題、未検証事項
- 次に行う具体的な作業を優先順位付きで記載

`RECENT_WORK.md`は累積日誌ではない。前回内容のうち今後も必要な恒久情報は`PROJECT_SUMMARY.md`へ移し、直近の引き継ぎ内容へ置き換える。

## Accuracy and Safety

- コード、設定、コマンド結果を確認せずに「完了」「成功」「動作確認済み」と書かない。
- テストを実行できなかった場合は、理由と未確認範囲を明記する。
- APIキー、パスワード、トークン、個人情報、秘密鍵、非公開URLなどの秘密情報をまとめへ記載しない。
- `.env`などGit管理外の環境依存情報は、値ではなく必要な変数名と設定方法だけを記載する。
- 変更したファイルは、可能な限りリポジトリ相対パスで示す。
- 一時ファイル、生成物、大容量データを引き継ぎ目的だけでGitへ追加しない。

## Before Commit or Push

1. `git status`またはGitHub DesktopのChangesで対象変更を確認する。
2. 関連するテストや検証を可能な範囲で実行する。
3. `docs/PROJECT_SUMMARY.md`を必要に応じて更新する。
4. `docs/RECENT_WORK.md`を必ず更新する。
5. まとめと実際の差分が一致しているか確認する。
6. 秘密情報や意図しないファイルが含まれていないか確認する。

まとめの更新依頼だけではcommitやpushの許可を意味しない。Git操作はユーザーが明示的に依頼した場合だけ行う。
